# Identifier

Identifiers have unique titles described by fully qualified names that indicate a logical hierarchy. An identifier has no parameters. Parentheses are added when necessary to separate.

There is a hierarchy of keywords in that some keywords are always followed by others.

## Examples

```nss
sel.path
sel.path().ext
```
